@model IEnumerable<ITP245_Fall2024_GraysonModel.Team>
@using System.Configuration;

@{
    ViewBag.Title = "Teams";
    ViewBag.ImagePath = ConfigurationManager.AppSettings["LogoImageLocation"]; // Getting the image path from the config
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New Team", "Create", null, new { @class = "btn btn-success" })
</p>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Logo</th>
            <th>@Html.DisplayNameFor(model => model.Name)</th>
            <th>@Html.DisplayNameFor(model => model.Division)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var team in Model)
        {
            <tr>
                <td>
                    @if (!string.IsNullOrEmpty(team.ImageLocation))
                    {
                        var image = string.Format("{0}{1}", ViewBag.ImagePath, team.ImageLocation);
                        <img id="Team Logo Image" height="100" width="90" style="border:solid" src="@Url.Content(image)" alt="Team Logo" />
                    }
                </td>
                <td>
                    <a href="#" class="team-link" data-team-id="@team.TeamId">@team.Name</a>
                </td>
                <td>
                    @(team.Division != null ? team.Division.Name : "N/A")
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = team.TeamId }, new { @class = "btn btn-primary btn-sm" })
                    @Html.ActionLink("Delete", "Delete", new { id = team.TeamId }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }
    </tbody>
</table>

<div id="team-details">
    <!-- Team details will be loaded here -->
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            // Handle click event on team links
            $(document).on('click', '.team-link', function (e) {
                e.preventDefault();
                var teamId = $(this).data('team-id');
                $.ajax({
                    url: '@Url.Action("Details", "Team")',
                    data: { id: teamId },
                    type: "GET",
                    success: function (data) {
                        $('#team-details').html(data);
                    },
                    error: function () {
                        alert('Error loading team details.');
                    }
                });
            });
        });
    </script>
}
